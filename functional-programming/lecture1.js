// Declerative

function greeting(name, greet) {
  return greet + "" + "Irfan";
}
const funcs = greeting(name, greet);
console.log(funcs);

/* Functional Programming =>
 *  1.Pure Function (1 to 1 function )
 *  2.Function Composition
 *  3.Shared State
 *  4.Mutable Data
 *  5.Side Effects
 */
